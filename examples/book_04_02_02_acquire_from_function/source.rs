fn main() {
    fn calculate_length(s: &String) -> usize {
        s.len()
    }
let <tspan data-hash="1">s1</tspan> = String::from("hello");

let <tspan data-hash="1">len</tspan> = calculate_length(&<tspan data-hash="1">s1</tspan>);
}